{% extends "TwigBundle::layout.html.twig" %}

{% block body %}
    <h1>Usignolo</h1>

    <ul id="issues-list">
        {% for issue in issues %}
        <li>{{ issue.title }}</li>
        {% endfor %}
    </ul>

    {{ form(form) }}
{% endblock %}
