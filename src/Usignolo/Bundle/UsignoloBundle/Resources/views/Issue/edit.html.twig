{% extends "UsignoloBundle::layout.html.twig" %}

{% block content %}
    <section>
        <h2>Issue edit</h2>

        {{ form(edit_form) }}

        <ul class="record_actions">
            <li><a href="{{ path('issue') }}">Back to the list</a></li>
            {% if not entity.complete %}<li>{{ form(complete_form) }}</li>{% endif %}
            <li>{{ form(delete_form) }}</li>
        </ul>
    </section>
{% endblock %}
